% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter2.R
\name{filter_redeemR_by_rules}
\alias{filter_redeemR_by_rules}
\title{Filter by predefined rules using GTsummary data}
\usage{
filter_redeemR_by_rules(
  redeemR_obj,
  rule = "ruleA",
  min_cells_per_variant = 2,
  QualifiedTotalCts = NULL,
  filter_name = NULL
)
}
\arguments{
\item{redeemR_obj}{A \code{redeemR} object.}

\item{rule}{Character; one of \code{"ruleA"}, \code{"ruleB"}, \code{"ruleC"}.}

\item{min_cells_per_variant}{Integer; minimum rows per variant (default 2).}

\item{QualifiedTotalCts}{Optional “QualifiedTotalCts” for depth rebuilding.}

\item{filter_name}{Optional label for matrix-dimension printing.}
}
\value{
The updated \code{redeemR} object.
}
\description{
Applies rule‑based filtering on \code{@GTsummary.filtered} using UMI counts and depth:
keeps rows with \code{Freq >= 2}; for \code{Freq == 1}, keeps only rows from \code{good_variants}
(variants meeting UMI‑based criteria) and with \code{depth <= max_depth}. Enforces minimum
rows per variant, then updates \code{@V.fitered} and rebuilds matrices.
}
