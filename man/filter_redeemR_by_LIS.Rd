% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter2.R
\name{filter_redeemR_by_LIS}
\alias{filter_redeemR_by_LIS}
\title{Filter by LIS score and VAF threshold (Wang et al., 2025, Genome Biology)}
\usage{
filter_redeemR_by_LIS(
  redeemR_obj,
  lis_cutoff = 0.6,
  hetero_cutoff = 0.05,
  min_cells_per_variant = 2,
  QualifiedTotalCts = NULL,
  filter_name = NULL
)
}
\arguments{
\item{redeemR_obj}{A \code{redeemR} object.}

\item{lis_cutoff}{Numeric LIS threshold (default 0.6). LIS ≈ mean(hetero) / var(hetero).}

\item{hetero_cutoff}{Numeric VAF (hetero) threshold (default 0.05).}

\item{min_cells_per_variant}{Integer; minimum rows per variant (default 2).}

\item{QualifiedTotalCts}{Optional “QualifiedTotalCts” table for depth rebuilding.}

\item{filter_name}{Optional label passed to \code{print_redeemR_matrix_dims}.}
}
\value{
The updated \code{redeemR} object.
}
\description{
Keeps variants with VAF (\code{hetero}) ≥ \code{hetero_cutoff} and LIS > \code{lis_cutoff},
enforces a minimum number of cells per variant, updates \code{@GTsummary.filtered} and
\code{@V.fitered}, then rebuilds matrices and depth matrix.
}
