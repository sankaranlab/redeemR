% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter2.R
\name{Create_redeemR_model}
\alias{Create_redeemR_model}
\title{Create a redeemR object from raw variant summaries}
\usage{
Create_redeemR_model(
  VariantsGTSummary = VariantsGTSummary,
  qualifiedCellCut = 10,
  VAFcut = 1,
  Cellcut = 2
)
}
\arguments{
\item{VariantsGTSummary}{A \code{data.frame} produced by \code{redeemR.read.trim()}, with attributes
\itemize{
\item \code{thr} (threshold),
\item \code{depth} (output of \code{DepthSummary()}),
\item (optionally) \code{edge_trim}, \code{path}, \code{combined}, \code{suffix}.
}}

\item{qualifiedCellCut}{Numeric; minimum \strong{median} mitochondrial coverage for a cell to be kept (default 10).}

\item{VAFcut}{Numeric in (0,1]; only variants with \strong{variant allele frequency} <= \code{VAFcut} are considered (default 1).}

\item{Cellcut}{Integer >= 1; only variants seen in at least \code{Cellcut} cells are kept (default 2).}
}
\value{
An object of S4 class \strong{redeemR}, with slots
\itemize{
\item \code{GTsummary.ini}, \code{V.ini}: the unfiltered genotype & variant tables
\item \code{GTsummary.filtered}, \code{V.fitered}: the post-filter tables
\item \code{CellMeta}, \code{DepthSummary}, \code{HomoVariants}, \code{UniqueV}, \code{para}, \code{attr}, ...
and with count matrices in \verb{@Cts.Mtx} / \verb{@Cts.Mtx.bi} populated.
}
}
\description{
This builds a redeemR S4 object by:
\enumerate{
\item filtering cells by median coverage (\code{qualifiedCellCut}),
\item filtering variants by cell-count and VAF (\code{Cellcut}, \code{VAFcut}),
\item initializing all slots (\code{GTsummary.ini}, \code{V.ini}, \code{GTsummary.filtered}, \code{V.fitered}, etc.),
\item running \code{Make_matrix()} to build count matrices,
\item and performing binomial noise removal.
}
}
\examples{
\dontrun{
  vgtsum <- redeemR.read.trim("path/to/data", thr="S", edge_trim=9)
  rObj   <- Create_redeemR_model(vgtsum, qualifiedCellCut=10, VAFcut=0.5, Cellcut=3)
  summary(rObj)
}

}
