% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter2.R
\name{update_redeemR_from_GTsummary}
\alias{update_redeemR_from_GTsummary}
\title{Update redeemR object from GTsummary.filtered (helper)}
\usage{
update_redeemR_from_GTsummary(
  redeemR_obj,
  QualifiedTotalCts = NULL,
  update_depth_matrix = TRUE
)
}
\arguments{
\item{redeemR_obj}{A \code{redeemR} object with a populated \code{@GTsummary.filtered}.}

\item{QualifiedTotalCts}{Optional “QualifiedTotalCts” table passed to \code{Add_DepthMatrix_filter2}.}
}
\value{
The updated \code{redeemR} object.
}
\description{
Synchronizes \code{@V.fitered} with \code{@GTsummary.filtered}: keeps only present variants,
computes per‑variant summaries (CellN, PositiveMean, PositiveMean_cts, maxcts, CV,
TotalVcount), merges them into \code{@V.fitered}, updates \code{@CellMeta} and \code{@UniqueV},
and rebuilds matrices and depth matrix.
}
\examples{
# ob@GTsummary.filtered <- dplyr::filter(ob@GTsummary.filtered, Freq >= 2)
# ob <- update_redeemR_from_GTsummary(ob)

}
